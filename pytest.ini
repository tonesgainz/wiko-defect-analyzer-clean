[pytest]
asyncio_mode = auto
markers =
    unit: fast, no network
    integration: hits Azure or cloud services
    manual: requires local files/hardware
addopts = -m "not integration and not manual"
